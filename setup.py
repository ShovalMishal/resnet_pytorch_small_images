from setuptools import setup, find_packages


setup(
    name='resnet_pytorch_small_images',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
    entry_points={
        'console_scripts': [
            # If your package includes any command-line scripts, list them here
        ],
    }
)
